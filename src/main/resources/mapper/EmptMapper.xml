<?xml version="1.0" encoding="utf-8" ?>
    <!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
    <mapper namespace="com.it.demo.dao.EmptDao">

    <select id="getAll" resultType="com.it.demo.domain.Empt">
        SELECT * from tbl_empt
    </select>

    <select id="selectByPrimaryKey" parameterType="java.lang.Integer" resultType="com.it.demo.domain.Empt">
        SELECT * FROM tbl_empt WHERE id = #{id}
    </select>

    </mapper>